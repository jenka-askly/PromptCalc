/**
 * Purpose: Provide minimal YAML parser type stubs for offline TypeScript builds.
 * Persists: None.
 * Security Risks: Describes parsing surface for policy configuration.
 */

export function parse(input: string): unknown;
