{
  "_fileHeader": {
    "purpose": "Configure the PromptCalc Azure Functions API workspace package.",
    "persists": "None.",
    "securityRisks": "Runs local Functions host and depends on environment variables for auth stubs."
  },
  "name": "@promptcalc/api",
  "version": "0.0.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && func start --typescript",
    "test": "vitest run"
  },
  "dependencies": {
    "@azure/data-tables": "^14.2.0",
    "@azure/functions": "^4.5.0",
    "@azure/storage-blob": "^12.19.0",
    "@promptcalc/logger": "file:../../shared/logger",
    "@promptcalc/types": "file:../../shared/types",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "typescript": "^5.4.5",
    "vitest": "^2.1.5"
  }
}
